plugins {
    id "com.android.library"
    id "org.jetbrains.kotlin.android"
}

apply from: "$rootDir/common.gradle"

android {
    namespace "dev.pimentel.sekret.navigation"
}

dependencies {
    implementation(project(":di"))
}
