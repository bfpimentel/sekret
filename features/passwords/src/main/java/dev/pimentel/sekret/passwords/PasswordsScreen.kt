package dev.pimentel.sekret.passwords

import androidx.compose.material.Button
import androidx.compose.material.Text
import androidx.compose.runtime.Composable

@Composable
fun PasswordsScreen() {
    Button(onClick = { /**/ }) {
        Text(text = "Navigate back to login")
    }
}
